package com.example.foldercleaner.ui.navigation

enum class AppDestination(val route: String) {
    Home(route = "home"),
    Folders(route = "folders"),
    IgnoredTypes(route = "ignored_types"),
    RunHistory(route = "run_history"),
    Schedule(route = "schedule"),
    Backup(route = "backup")
}
